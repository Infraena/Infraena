apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "idp.fullname" . }}-config
  labels: {{- include "idp.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.configmap }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
